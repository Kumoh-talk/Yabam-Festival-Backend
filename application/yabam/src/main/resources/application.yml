spring:
  application:
    name:
      yabam
  config:
    activate:
      on-profile: local
    import: configserver:http://localhost:8087

---

spring:
  application:
    name:
      yabam
  config:
    activate:
      on-profile: dev
    import: configserver:http://localhost:8087